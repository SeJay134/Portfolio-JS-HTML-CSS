# Future Test Scenarios

Not-yet-accepted scenarios live here and are not executed as passing CI tests.

Current areas:
- production Ollama/RAG factual evaluation and threshold calibration;
- stable production API/readiness behavior;
- real iPhone Safari and Android Chrome keyboard/safe-area checks;
- optional Three.js/WebGL context-loss, disposal, and performance checks;
- streaming chat protocol/cancellation if streaming is introduced.

When implemented, convert each scenario into the appropriate executable suite and
remove the stale future entry.
